import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    title: {
        fontSize: 16,
        fontWeight: 'bold',
        color: '#e32439',
        marginTop: 10,
        marginLeft: 15
    }
});
